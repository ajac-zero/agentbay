apiVersion: batch/v1
kind: CronJob
metadata:
  name: dispatch-reconciler
  namespace: agents
spec:
  schedule: "*/30 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app.kubernetes.io/name: dispatch-reconciler
        spec:
          serviceAccountName: dispatch-reconciler
          restartPolicy: OnFailure
          containers:
            - name: reconciler
              image: ghcr.io/example/dispatch:latest
              imagePullPolicy: IfNotPresent
              command: ["node", "dist/reconcile.js"]
              env:
                - name: DISPATCH_KUBE_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: LOG_LEVEL
                  value: info
