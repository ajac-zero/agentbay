{{- if and .Values.secrets.create (not .Values.secrets.existingSecret) -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace (include "dispatch.fullname" .) -}}
{{- $existingAdminToken := "" -}}
{{- if and $existing $existing.data $existing.data.DISPATCH_ADMIN_TOKEN -}}
{{- $existingAdminToken = $existing.data.DISPATCH_ADMIN_TOKEN | b64dec -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dispatch.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dispatch.labels" . | nindent 4 }}
type: Opaque
stringData:
  DISPATCH_ADMIN_TOKEN: {{ default (default (randAlphaNum 32) $existingAdminToken) .Values.admin.token | quote }}
  {{- range $key, $value := omit .Values.secrets.data "DISPATCH_ADMIN_TOKEN" }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
