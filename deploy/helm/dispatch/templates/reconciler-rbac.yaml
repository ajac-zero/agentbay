{{- if and .Values.reconciler.enabled .Values.reconciler.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "dispatch.fullname" . }}-reconciler-sandboxclaims
  namespace: {{ include "dispatch.claimsNamespace" . }}
  labels:
    {{- include "dispatch.labels" . | nindent 4 }}
    app.kubernetes.io/component: reconciler
rules:
  - apiGroups: ["extensions.agents.x-k8s.io"]
    resources: ["sandboxclaims"]
    verbs: ["list", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "dispatch.fullname" . }}-reconciler-sandboxclaims
  namespace: {{ include "dispatch.claimsNamespace" . }}
  labels:
    {{- include "dispatch.labels" . | nindent 4 }}
    app.kubernetes.io/component: reconciler
subjects:
  - kind: ServiceAccount
    name: {{ include "dispatch.reconciler.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "dispatch.fullname" . }}-reconciler-sandboxclaims
{{- end }}
