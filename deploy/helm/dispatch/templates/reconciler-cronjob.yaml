{{- if .Values.reconciler.enabled -}}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "dispatch.fullname" . }}-reconciler
  namespace: {{ include "dispatch.claimsNamespace" . }}
  labels:
    {{- include "dispatch.labels" . | nindent 4 }}
    app.kubernetes.io/component: reconciler
spec:
  schedule: {{ .Values.reconciler.schedule | quote }}
  concurrencyPolicy: {{ .Values.reconciler.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.reconciler.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.reconciler.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            {{- include "dispatch.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: reconciler
        spec:
          serviceAccountName: {{ include "dispatch.reconciler.serviceAccountName" . }}
          restartPolicy: OnFailure
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.podSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: reconciler
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- with .Values.securityContext }}
              securityContext:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              command: ["node", "dist/reconcile.js"]
              env:
                - name: DISPATCH_KUBE_NAMESPACE
                  value: {{ include "dispatch.claimsNamespace" . | quote }}
                - name: DISPATCH_SANDBOX_CLAIM_API_VERSION
                  value: {{ .Values.claims.apiVersion | quote }}
                - name: DISPATCH_RECONCILER_GRACE_MINUTES
                  value: {{ .Values.reconciler.graceMinutes | quote }}
                {{- with .Values.reconciler.extraEnv }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
              {{- with .Values.reconciler.extraEnvFrom }}
              envFrom:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- with .Values.reconciler.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
{{- end }}
